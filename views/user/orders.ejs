<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>My Orders | ATOM</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">

  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">

  <style>
    body { font-family: "Segoe UI", sans-serif; background:#fff; }
    .account-wrapper { padding:60px 0 80px; }
    .order-card {
      background:#f8f9fa;
      border-radius:12px;
      padding:20px;
      transition: box-shadow .3s;
    }
    .order-card:hover { box-shadow:0 4px 20px rgba(0,0,0,.1); }
    .status-badge { text-transform: capitalize; }
    .page-title { font-size:28px; font-weight:600; }
    .pagination .page-link {
      border-radius:8px; margin:0 4px; color:#198754;
    }
    .pagination .active .page-link {
      background:#198754; border-color:#198754; color:#fff;
    }
  </style>
</head>

<body>

<%- include('./userPartials/navbar') %>

<div class="container account-wrapper">
  <div class="row">

    <%- include('./userPartials/sidebar') %>

    <div class="col-lg-9 col-md-8">
      <div class="page-title mb-4">My Orders</div>

      <% if (!orders || orders.length === 0) { %>
        <p class="text-muted">No orders found.</p>
      <% } else { %>

        <div class="row g-4">
          <% orders.forEach(order => { %>
            <div class="col-md-6">
              <div class="order-card h-100">
                <p class="fw-bold mb-1">Order ID: <%= order.orderNumber %></p>
                <p class="mb-1">
                  Ordered on:
                  <%= new Date(order.created_at).toDateString() %>
                </p>

                <span class="badge bg-success status-badge mb-3">
                  <%= order.status %>
                </span>

                <div class="d-flex gap-2 mt-3">
                  <a href="/user/orders/<%= order._id %>"
                     class="btn btn-outline-success btn-sm">
                    View Details
                  </a>

                  <% if (order.status === 'delivered') { %>
                    <button
                      class="btn btn-outline-danger btn-sm"
                      onclick="handleReturn('<%= order._id %>')">
                      Return
                    </button>
                  <% } else if (['placed','confirmed','shipped'].includes(order.status)) { %>
                    <button
                      class="btn btn-outline-danger btn-sm"
                      onclick="handleCancel('<%= order._id %>')">
                      Cancel
                    </button>
                  <% } %>
                </div>
              </div>
            </div>
          <% }) %>
        </div>

      <% } %>

      <% if (totalPages > 1) { %>
        <nav class="d-flex justify-content-center mt-5">
          <ul class="pagination">

            <li class="page-item <%= currentPage === 1 ? 'disabled' : '' %>">
              <a class="page-link" href="?page=<%= currentPage - 1 %>">&laquo;</a>
            </li>

            <% for (let i = 1; i <= totalPages; i++) { %>
              <li class="page-item <%= currentPage === i ? 'active' : '' %>">
                <a class="page-link" href="?page=<%= i %>"><%= i %></a>
              </li>
            <% } %>

            <li class="page-item <%= currentPage === totalPages ? 'disabled' : '' %>">
              <a class="page-link" href="?page=<%= currentPage + 1 %>">&raquo;</a>
            </li>

          </ul>
        </nav>
      <% } %>

    </div>
  </div>
</div>

<%- include('./userPartials/footer') %>

<script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
<script>
  function handleCancel(orderId) {
    // call cancel API
  }
  function handleReturn(orderId) {
    // call return API
  }
</script>

</body>
</html>
