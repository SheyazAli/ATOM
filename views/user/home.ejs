<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>ATOM | Home</title>

  <!-- Bootstrap -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">

  <!-- Bootstrap Icons -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css" rel="stylesheet">

  <style>
  body {
    font-family: 'Segoe UI', sans-serif;
  }


  /* HERO */
.hero {
  position: relative;
  height: 520px;
  background: url("/uploads/SALE.jpg") center center / cover no-repeat;
  display: flex;
  align-items: center;
  justify-content: flex-end;
}

/* overlay */
.hero::before {
  content: "";
  position: absolute;
  inset: 0;
  background: rgba(0,0,0,0.25);
}

/* hero text block */
.hero-content {
  position: relative;
  z-index: 2;
  color: #fff;
  max-width: 600px;
  margin-right: 80px;
  text-align: left; /* 👈 keeps text + button aligned */
}

.hero h1 {
  font-weight: 800;
  letter-spacing: 2px;
  font-size: 42px;
  line-height: 1.3;
  margin: 0; /* 👈 IMPORTANT: remove left margin */
}


  /* CATEGORY GRID */
  .category-card {
    position: relative;
    overflow: hidden;
    height: 100%;
  }

  .category-card img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: transform 0.4s ease;
  }

  .category-card:hover img {
    transform: scale(1.05);
  }

  .category-label {
    position: absolute;
    bottom: 20px;
    left: 20px;
    background: rgba(0,0,0,0.7);
    color: #fff;
    padding: 6px 14px;
    font-size: 12px;
    letter-spacing: 1px;
  }

  /* FORCE EQUAL HEIGHT ROWS */
  .category-row {
    display: grid;
    grid-template-columns: 1fr 2fr;
    gap: 24px;
  }

  .category-bottom {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 24px;
    margin-top: 24px;
  }

  /* SALE */
  .sale {
    padding: 60px 0;
    background: #f8f8f8;
  }


</style>

</head>
<body>

<!-- NAVBAR -->
<%- include('./userPartials/navbar') %>

<!-- HERO -->
<section class="hero">
  <div class="hero-content">
    <h1>
      COMFORTABLE. CONFIDENT <br>
      & SEASON READY
    </h1>

        <a href="/user/products" class="btn btn-dark mt-4 px-4 py-2">
      SHOP NOW
    </a>
  </div>
</section>


<!-- STYLE FOR EVERY STORY -->
<section class="py-5">
  <div class="container">
    <h5 class="text-center mb-4">STYLE FOR EVERY STORY</h5>

    <div class="row g-4">
      <div class="col-md-5">
        <div class="category-card">
          <img src="/uploads/polo.png">
          <div class="category-label">POLOS</div>
        </div>
      </div>

      <div class="col-md-7">
        <div class="category-card">
          <img src="/uploads/swtees.png">
          <div class="category-label">SWEATSHIRTS</div>
        </div>
      </div>

      <div class="col-md-7">
        <div class="category-card">
          <img src="/uploads/hoodii cropd.png">
          <div class="category-label">HOODIES</div>
        </div>
      </div>
<div class="col-md-5">
        <div class="category-card">
          <img src="/uploads/oversiz.png">
          <div class="category-label">OVERSIZED</div>
        </div>
      </div>


    </div>
  </div>
</section>

<!-- SALE -->
<section class="sale text-center">
  <h3>SALE IS LIVE</h3>
  <p>Up to 50% off on selected styles</p>
  <button class="btn btn-dark">SHOP SALE</button>
</section>

<!-- FOOTER -->
<%- include('./userPartials/footer') %>



<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
