<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Forgot Password | ATOM</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">

  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">

  <style>
    body {
      font-family: 'Segoe UI', sans-serif;
    }

    .login-wrapper {
      min-height: 100vh;
      background: url("/uploads/SALE.jpg") center / cover no-repeat;
      position: relative;
      display: flex;
      align-items: center;
      justify-content: center;
    }

    .login-wrapper::before {
      content: "";
      position: absolute;
      inset: 0;
      background: rgba(0, 0, 0, 0.6);
    }

    .login-card {
      position: relative;
      z-index: 2;
      background: #fff;
      max-width: 360px;
      width: 100%;
      padding: 26px 24px;
      border-radius: 12px;
      box-shadow: 0 15px 50px rgba(0,0,0,0.35);
      text-align: center;
    }

    .form-control {
      height: 40px;
      font-size: 13px;
      border-radius: 6px;
    }

    .btn-submit {
      background: #008060;
      color: #fff;
      border: none;
      height: 42px;
      border-radius: 6px;
    }
  </style>
</head>

<body>

<section class="login-wrapper">
  <div class="login-card">

    <h4>Forgot Password</h4>
    <p class="text-muted small">Enter your registered email address</p>

    <% if (error) { %>
      <div class="alert alert-danger py-1"><%= error %></div>
    <% } %>

    <form action="/user/forgot-password" method="POST">
      <div class="mb-3 text-start">
        <label class="form-label">Email ID</label>
        <input type="email" name="email" class="form-control" required>
      </div>

      <button type="submit" class="btn btn-submit w-100">
        Continue
      </button>
    </form>

    <div class="mt-3">
      <a href="/user/signup" class="small">Can’t find your account? Sign up</a>
    </div>

  </div>
</section>

</body>
</html>
