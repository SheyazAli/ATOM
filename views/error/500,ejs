<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>500 | Internal Server Error - ATOM</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />

  <!-- Bootstrap -->
  <link
    href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css"
    rel="stylesheet"
  />

  <style>
    body {
      font-family: "Segoe UI", sans-serif;
      background: #ffffff;
      color: #111;
    }

    .error-wrapper {
      min-height: 70vh;
      display: flex;
      align-items: center;
      justify-content: center;
      text-align: center;
      padding: 60px 15px;
    }

    .error-box h1 {
      font-size: 72px;
      font-weight: 700;
      margin-bottom: 10px;
    }

    .error-box h4 {
      font-weight: 600;
      margin-bottom: 12px;
    }

    .error-box p {
      color: #666;
      max-width: 420px;
      margin: 0 auto 25px;
      font-size: 15px;
    }

    .btn-primary {
      background-color: #4caf50;
      border-color: #4caf50;
      border-radius: 10px;
      padding: 10px 22px;
      font-size: 15px;
    }

    .btn-outline-secondary {
      border-radius: 10px;
      padding: 10px 22px;
      font-size: 15px;
    }

    .error-code {
      font-size: 12px;
      color: #999;
      margin-top: 18px;
    }
  </style>
</head>

<body>

  <!-- ================= NAVBAR ================= -->
  <%- include('../userPartials/navbar') %>

  <!-- ================= ERROR CONTENT ================= -->
  <section class="error-wrapper">
    <div class="error-box">

      <!-- Icon (simple emoji / can replace with SVG later) -->
      <div class="mb-3" style="font-size: 42px;">🧠</div>

      <h1>500</h1>
      <h4>Internal Server Error</h4>

      <p>
        Something went wrong on our end.  
        We’re working to fix it as quickly as possible.
      </p>

      <div class="d-flex justify-content-center gap-3 flex-wrap">
        <button onclick="location.reload()" class="btn btn-primary">
          Reload Page
        </button>

        <a href="/" class="btn btn-outline-secondary">
          Go to Homepage
        </a>
      </div>

      <div class="error-code">
        Error Code: 500-ATOM
      </div>
    </div>
  </section>

  <!-- ================= FOOTER ================= -->
  <%- include('../userPartials/footer') %>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
